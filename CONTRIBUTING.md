# 기여 안내

VeilView는 가벼운 수동 브라우저 오버레이를 목표로 하는 프로젝트입니다.
기능을 추가하거나 수정할 때는 아래 원칙을 지켜주세요.

## 개발 및 빌드

기본 빌드:

```powershell
.\build.ps1
```

또는 CMD 파일 실행:

```powershell
.\BUILD_SINGLE_EXE.cmd
```

성공하면 `dist\VeilView.exe` 단일 실행 파일이 생성됩니다.

## 개발 원칙

- 자동 입력, 반복 매크로, 글로벌 키보드 훅, DLL 인젝션, 패킷 변조 기능은 추가하지 않습니다.
- 다른 프로세스의 메모리를 읽거나 쓰는 기능은 추가하지 않습니다.
- UI 흐름은 `키보드 보존` / `주소 입력` / `보존 복귀` 구조를 유지합니다.
- WebView2 초기화 오류는 숨기지 말고 사용자에게 원인을 보여줍니다.
- 배포 파일은 가능하면 단일 실행 파일을 우선합니다.
- 사용자가 `VeilView.exe` 하나만 받아도 실행할 수 있는 구조를 유지합니다.

## Pull Request 체크리스트

- Windows x64에서 `BUILD_SINGLE_EXE.cmd` 빌드를 확인했습니다.
- `dist\VeilView.exe`를 다른 폴더로 단독 이동한 뒤 실행을 확인했습니다.
- 새 탭에서 열기 / 새 창에서 열기 요청이 VeilView 내부 탭으로 열리는지 확인했습니다.
- 투명도 `0%`, `30%`, `70%` 버튼이 정상 동작하는지 확인했습니다.
- WebView2 Runtime이 없거나 손상된 경우의 오류 메시지가 사용자가 이해할 수 있게 표시되는지 확인했습니다.
