# VeilView {{TAG}}

VeilView는 키보드 포커스를 기존 활성 창에 보존하면서 웹페이지를 마우스로 탐색할 수 있는 Windows용 플로팅 탭 브라우저입니다.

## 이번 버전의 주요 변경점

- VeilView 내부 탭 기능을 추가했습니다.
- 웹페이지의 `새 탭에서 열기`, `새 창에서 열기`, `target="_blank"`, `window.open()` 요청을 외부 브라우저가 아니라 VeilView 내부 탭으로 열도록 변경했습니다.
- 투명도 조절을 `0%`, `30%`, `70%` 세 단계 버튼으로 정리했습니다.
- 기본 투명도를 `0%`로 변경해 처음 실행 시 완전 불투명 상태로 시작합니다.
- 열린 탭 목록, 선택된 탭, 창 위치, 창 크기, 투명도 저장 기능을 추가했습니다.
- 단일 실행 파일 배포 구성을 정리했습니다.

## 다운로드

일반 사용자는 아래 파일만 받으면 됩니다.

```text
VeilView.exe
```

압축 파일을 선호하는 경우 `VeilView-win-x64.zip`을 받아 압축을 풀고 실행하면 됩니다.

## 실행 전 확인 사항

- Windows 10 1809 이상 또는 Windows 11 권장
- Microsoft Edge WebView2 Runtime 필요
- 대부분의 Windows 10/11 환경에는 WebView2 Runtime이 이미 설치되어 있습니다.
- 실행 시 WebView2 오류가 발생하면 저장소의 `REPAIR_WEBVIEW2_RUNTIME.cmd`를 실행하거나 WebView2 Runtime을 설치해 주세요.

## 기본 사용법

1. 키보드 입력을 유지하고 싶은 창을 먼저 클릭합니다.
2. `VeilView.exe`를 실행합니다.
3. `키보드 보존` 상태에서 VeilView를 마우스로 클릭하거나 스크롤합니다.
4. 주소창이나 웹페이지 입력칸에 글자를 입력해야 할 때만 `주소 입력`을 누릅니다.
5. 입력이 끝나면 `보존 복귀`로 돌아갑니다.

## 주의 사항

VeilView는 다른 프로그램의 메모리를 읽거나, 패킷을 변조하거나, DLL을 주입하거나, 자동 입력을 수행하지 않습니다.
다만 각 서비스나 게임의 이용 약관은 사용자가 직접 확인해야 합니다.
