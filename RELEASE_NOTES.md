# VeilView {{TAG}} 배포

이번 버전은 마우스 제스처 기본값과 투명도 설정 방식을 정리한 패치입니다.

## 주요 변경 사항

- `항상 위` 상태에서 설정창이 브라우저 뒤에 가려질 수 있던 문제를 수정했습니다.
- `제스처` 설정창과 `투명도` 설정창이 항상 위 브라우저보다 위에 뜨도록 처리했습니다.
- `주소 입력` / `보존 복귀` 대신 `직접 입력` / `작업창 복귀` 표현을 사용합니다.
- `↔` 제스처를 추가하고, 기본 동작을 `직접 입력` / `작업창 복귀` 전환으로 설정했습니다.
- `┌`, `┐` 탭 이동 기본 방향을 교체했습니다.
- `┘`, `└` 기본 동작을 둘 다 탭 닫기로 변경했습니다.
- 투명도 3단계 버튼을 제거하고, 불투명도 100%~30% 슬라이더 설정창으로 변경했습니다.

## 기본 제스처

| 제스처 | 기본 동작 |
|---|---|
| `←` | 이전 페이지 |
| `→` | 다음 페이지 |
| `↕` | 새로고침 |
| `↔` | 직접 입력 / 작업창 복귀 전환 |
| `┌` | 오른쪽 탭 이동 |
| `┐` | 왼쪽 탭 이동 |
| `┘` | 탭 닫기 |
| `└` | 탭 닫기 |

## 다운로드

- `VeilView.exe`: 단일 실행 파일
- `VeilView-win-x64.zip`: exe 압축본

실행 PC에는 Microsoft Edge WebView2 Runtime이 필요합니다. 대부분의 Windows 10/11에는 이미 설치되어 있습니다.
