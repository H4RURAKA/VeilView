# VeilView {{TAG}} 배포

VeilView {{TAG}}는 우클릭 드래그 기반 **마우스 제스처**를 추가한 버전입니다.

## 주요 변경 사항

- 브라우저 화면 위에서 오른쪽 마우스 버튼을 누른 채 드래그해 동작을 실행할 수 있습니다.
- `←`, `→`, `↕`, `┌`, `┐`, `┘`, `└` 형태의 제스처를 인식합니다.
- 상단 `제스처` 버튼에서 각 제스처에 연결할 동작을 직접 선택할 수 있습니다.
- 왼쪽/오른쪽 탭 이동은 루프 방식으로 동작합니다.
  - 첫 번째 탭에서 왼쪽 탭 이동 → 마지막 탭
  - 마지막 탭에서 오른쪽 탭 이동 → 첫 번째 탭

## 기본 제스처 매핑

| 제스처 | 기본 동작 |
| --- | --- |
| `←` | 이전 페이지 |
| `→` | 다음 페이지 |
| `↕` | 새로고침 |
| `┌` | 왼쪽 탭 이동 |
| `┐` | 오른쪽 탭 이동 |
| `┘` | 탭 닫기 |
| `└` | 동작 없음 |

## 다운로드

일반 사용자는 아래 파일 중 하나를 받으면 됩니다.

```text
VeilView.exe
VeilView-win-x64.zip
```

`VeilView.exe`는 단일 실행 파일 배포용입니다. 바탕화면이나 원하는 폴더로 파일 하나만 옮겨 실행할 수 있습니다.

## 실행 요구 사항

- Windows 10 1809 이상 또는 Windows 11
- x64 환경 권장
- Microsoft Edge WebView2 Runtime 필요

대부분의 Windows 10/11 환경에는 WebView2 Runtime이 이미 설치되어 있습니다. 실행 시 WebView2 관련 오류가 발생하면 `REPAIR_WEBVIEW2_RUNTIME.cmd`를 실행하거나 Microsoft Edge WebView2 Runtime을 설치하세요.

## 참고

VeilView는 다른 프로세스의 메모리 읽기, 패킷 변조, DLL 인젝션, 글로벌 키보드 훅, 자동 입력 매크로 기능을 포함하지 않습니다.
